# 智能客服知识库系统 — 开发进度

> 活文档：每完成一步就更新。配套《总体开发计划.md》（架构与计划）、《智能客服知识库系统需求分析与开发计划_Qt.md》（需求）。
>
> 最近更新：2026-06-17

---

## 一、当前状态

- **阶段**：第 0 步「脚手架与空壳」基本完成；第一阶段·**模块 1（登录鉴权 + RBAC）、模块 2（系统管理）、模块 3（知识基础·分类/标签）、模块 4（知识管理后台）、模块 5（检索 + 详情）、模块 6（知识互动）均已完成（模块 5、6 后端冒烟通过，Qt 客户端待用户点测）**。模块 6 新增包 `com.kb.interaction`：实体/Mapper `KbFavorite`/`KbShare`/`KbLike`（纯表，仅 createTime）+ `KbComment`（纯表 + `@TableLogic deleted`）；`InteractionService`/`InteractionController`（前缀 `/interaction`）实现 收藏（幂等、计数）、点赞/点踩（ARTICLE/COMMENT，赞↔踩双向搬移计数）、评论（顶层 + parentId 回复、树形、本人可删）、分享给同事（`kb_share` 即站内通知）、收件箱/已发出/未读数/标已读、收件人选项 `/interaction/users`（避开 `system:user:list`）、互动初态 `/interaction/state`；计数维护在 `kb_article` 用 `setSql` 自增（不动 `update_time`）、`GREATEST` 防负。Flyway `V6` 幂等补 `interaction:like`（菜单 201 下 id 2012）并授 ADMIN（迁移链 v5→v6）。Qt 客户端：详情弹窗加互动条（收藏/赞/踩/分享）+ 评论区（树形 + 发表/回复）、新增 `ShareDialog`/`FavoritePage`(route `favorite`)/`SharePage`(route `share`，收件箱/发出双 Tab + 未读数)。下一步进入模块 7「数据统计」。
- **客户端**：✅ 已在 `knowledgeAnswer/` 上扩展为可运行骨架（Qt 6.11.1 / MinGW / C++17）：`core/`（ApiClient / Session / Settings / Logger / Notify）+ `app/`（LoginDialog / MainWindow / PageRouter / Application / PlaceholderPage / SystemAdminPage）+ `resources/`（`app.qss` 主题）。登录调真实 `POST /auth/login` 拿 JWT → `GET /auth/me` 填会话 → 主窗口按返回的 16 菜单建左侧导航树 + `QStackedWidget` 路由；系统管理菜单已接入人员、角色、菜单按钮权限、系统日志初版页面。已用 Qt 自带 CMake/MinGW 编译通过。
- **后端**：✅ Spring Boot 3.3.5 骨架 + 模块 1 鉴权/RBAC 已验证通过（2026-06-16，IDEA 自带 Maven）：Flyway 自动建 26 表 + RBAC 种子（`now at version v2`）；`/api/actuator/health`=UP；`/auth/login` 已从 DB 查 `sys_user` 并用 BCrypt 校验 `admin/123456`，签发 JWT；`JwtAuthenticationFilter` 每请求解析 token 并加载用户权限码写入 authorities；`/auth/me` 从 `sys_user/sys_role/sys_permission` 装配当前用户、角色、21 权限码与 6 个顶级目录/16 个菜单；禁用/过期账号无法登录，旧 token 也不会继续通过。已 `compile` 通过并完成登录、错误密码、`/auth/me` 冒烟。
- **AI 服务**：✅ 已建 `ai-service/` 占位骨架（Python 3.11 + FastAPI，**conda 环境 `kb-ai`**）：`app/main.py` 装配路由，`/health` 可用；`/ai/parse`·`/ai/embed`·`/ai/qa` 为契约占位（返回 501，`app/schemas/ai.py` 的 pydantic 模型即 Java↔FastAPI 契约）。已 `py_compile` 通过；`/health` 运行冒烟待建 conda 环境后验证（本机 base 环境未装 fastapi）。具体能力二期（模块 9/10）实现。

---

## 二、总体进度清单

### 第 0 步 脚手架与空壳
- [x] docs/总体开发计划.md
- [x] docs/开发进度.md
- [x] docs/数据库表清单.md
- [x] docs/API契约.md
- [x] 根目录 README.md、.gitignore
- [x] 后端 `server/` 骨架（Spring Boot + common/config/security + auth(login/me) + Flyway V1 建表）
- [x] 客户端 `knowledgeAnswer/` 骨架（core + app + resources 主题；登录 → `/auth/me` → 导航树 → 占位页路由。`common/` 复用控件与各模块真实页随对应模块再建）
- [x] AI 服务 `ai-service/` 占位（FastAPI `/health` + `/ai/*` 契约占位；conda 环境 `kb-ai`）
- [x] 团队仓库 `auto-service-knowledge-base/` 首次结构落地并提交（构建/缓存产物不入库）

### 第一阶段（知识中台核心）
- [x] 模块 1 登录鉴权 + RBAC（后端 DB+BCrypt 登录、JWT、`/auth/me` DB 装配菜单+权限码、authorities 权限码注入；客户端骨架已可消费）
- [x] 模块 2 系统管理（机构 / 人员 / 角色 / 菜单按钮权限 / 登录·操作日志；后端接口冒烟 17/0 通过，Qt 客户端四页点测通过）
- [x] 模块 3 知识基础（分类 / 标签；后端分类树 + 标签 CRUD 冒烟通过，Qt 客户端双 Tab 点测通过）
- [x] 模块 4 知识管理后台（知识 CRUD + 版本/审核/上下线状态机 + 标签挂接 + 附件上传下载；后端编译 + 接口冒烟全过，Qt 客户端「知识管理 / 审核中心」两页点测通过）
- [x] 模块 5 检索 + 详情（后端 `/search/article` 全文检索 + `/knowledge/article/{id}/view` 浏览埋点；接口冒烟全过，Qt 客户端「知识检索」页 + 详情弹窗待用户点测）
- [x] 模块 6 知识互动（后端 `/interaction/*` 收藏/点赞·点踩/评论/分享 + 站内通知 + Flyway V6 补 `interaction:like`；接口冒烟全过，Qt 客户端详情弹窗互动条+评论区、「我的收藏」「我的分享」两页待用户点测）
- [ ] 模块 7 数据统计
- [ ] 模块 8 知识能力开放 API

### 第二阶段（智能与实时）
- [ ] 模块 9 智能问答（FastAPI）
- [ ] 模块 10 客服实时辅助（CC / ASR / 实时推荐 / 坐席面板）

---

## 三、决策记录

| 日期 | 决策 |
|---|---|
| 2026-06-15 | 客户端 UI 用 **Qt Widgets**（用户已重建为 Widgets 工程，原 Qt Quick/QML 工程弃用） |
| 2026-06-15 | 客户端工程目录沿用 **`knowledgeAnswer/`**，不另建 `client/` |
| 2026-06-15 | 后端 **Java + Spring Boot**（`server/`，包名 `com.kb`），AI 部分由 **Python + FastAPI**（`ai-service/`）提供接口 |
| 2026-06-15 | **Python / AI 服务环境用 conda 管理**（用户要求） |
| 2026-06-15 | ORM MyBatis-Plus；建表用 Flyway；鉴权 JWT + RBAC（权限码驱动菜单/按钮） |
| 2026-06-15 | 一期检索用 MySQL（不上 ES）；文件先落本地目录（`FileStorage` 抽象，后续可换 MinIO） |
| 2026-06-16 | 团队仓库目录确定为 `auto-service-knowledge-base/`；上层 `thePro/` 仅保留 `.claude/` 与该仓库目录，源码、文档、图片全部纳入仓库管理 |

---

## 四、待确认 / 待验证

- 工具链核实（2026-06-16）：JDK 17.0.8 ✅、MySQL 8.0.42 ✅、git 2.49 ✅、conda 25.9.1 ✅、Python 3.13.9（计划用 conda 建 3.11 环境，二期再说）。**Maven**：系统 PATH 无 `mvn`，但已用 **IDEA 自带 Maven** 完成依赖拉取与启动验证（命令行构建需要再装）；**cmake 不在 PATH**（用 Qt Creator 自带 CMake 构建客户端即可）。
- MySQL 8.0.42 已安装 ✅；已建 `kb` 库 + **本项目专属账号 `kb`**（`GRANT ALL ON kb.*`），`application.yml` 用 `kb`/`123456` 连接，**不用 root**（root 仅用于建库建号）。
- ✅ 模块 2 接口联调已验证（2026-06-16，重启 IDEA 后端触发 Flyway `V3__seed_module2_permissions.sql`）：admin token 冒烟 17/0 全绿 —— `/system/org/tree`、`/system/user`、`/system/role`、`/system/permission/tree`、`/system/log/login`、`/system/log/operation` 均 `code=0`；机构 POST→GET→PUT→DELETE 闭环通过；POST/PUT/DELETE 已写入 `sys_operation_log`、登录已写入 `sys_login_log`。Qt 客户端四页（人员/角色/菜单按钮权限/系统日志）逐页点测通过。
- ✅ 模块 3 接口联调已验证（2026-06-16，重启 IDEA 后端触发 Flyway `V4__seed_module3_permissions.sql`）：admin token 冒烟通过 —— `/knowledge/category/tree`、`/knowledge/tag` 均 `code=0`；分类 POST 建根/子 → `DELETE 根` **级联删除**根连同全部子孙分类（删除前 root/child/grand 各 1 行、删除后均 0 行，`code=0`）；标签 POST 建标签 → 重名 POST 返回 `code=1001 标签名称已存在`；**软删后同名重建**返回 `code=1001 数据已存在或违反唯一约束`（全局 `DataIntegrityViolationException` 兜底，不再冒泡 5000）。Qt 客户端「分类标签管理」双 Tab（分类树 新增根/子/编辑/级联删除含「输入确定删除」二次确认、标签 取色器/色块表）点测通过。
- Qt 客户端已在仓库内重新 Configure 到 `knowledgeAnswer/build/Desktop_Qt_6_11_1_MinGW_64_bit-Debug` 并编译通过；该目录为本地构建产物，受 `.gitignore` 排除。

---

## 五、变更日志

- **2026-06-17 · 模块 6 知识互动（后端完成 / 客户端待点测）**
  - 后端新增包 `com.kb.interaction`：实体/Mapper `KbFavorite`(`kb_favorite`)、`KbShare`(`kb_share`)、`KbLike`(`kb_like`)（纯表，无 BaseEntity，仅 createTime，镜像 `KbViewLog`）、`KbComment`(`kb_comment`，纯表 + 手写 `@TableLogic deleted`，因该表无 create_by/update_by/update_time)；DTO `FavoriteRequest/LikeRequest/CommentRequest/ShareRequest` + `FavoriteVO/CommentVO(树形)/ShareVO/UserOptionVO/InteractionStateVO`；`InteractionService` + `InteractionController`（前缀 `/interaction`）。
  - 收藏：`POST /interaction/favorite` 按 `uk_fav_art_user` 查重**幂等**（已收藏仅更新收藏夹、不重复计数）+ `favorite_count+1`；`DELETE /interaction/favorite/{articleId}` 删生效时 `GREATEST(-1,0)`；`GET /interaction/favorite` 分页富集标题/分类名。
  - 点赞/点踩：`POST /interaction/like {targetType(ARTICLE/COMMENT),targetId,type(1/-1)}` —— 无既有→插入+计数；同 type 再点→取消；**异 type→改表态并双向搬移计数**（ARTICLE 在 like/dislike 间，COMMENT 仅 `like_count` 跟随 type=1）；返回最新 `InteractionStateVO`。
  - 评论：`POST /interaction/comment`（parentId 回复校验同 article）+ `comment_count+1`；`DELETE /interaction/comment/{id}` 仅作者本人（否则 `2003`）逻辑删 + `GREATEST(-1,0)`；`GET /interaction/comment?articleId=`（权限 `knowledge:search`）返回 NORMAL 未删评论的内存树（顶层 + children）。
  - 分享/站内通知：`POST /interaction/share`（校验收件人 ENABLED）写 `kb_share`，该行即站内通知；`GET /interaction/share/inbox`(收件箱)/`/share/sent`(发出) 分页富集、`PUT /interaction/share/{id}/read` 标已读、`GET /interaction/share/unread-count` 未读数；`GET /interaction/users` 返回启用用户 id+姓名供坐席选人（避开 `system:user:list`）。`GET /interaction/state?articleId=` 返回收藏/我的点赞态/四项计数。计数全用 `kb_article` 上的 `setSql` 自增（不动 `update_time`）/ `GREATEST` 防负，无 `share_count` 列。
  - Flyway `V6__seed_module6_permissions.sql`（幂等，仅权限码）：补 `interaction:like`（菜单 201「知识检索」下 BUTTON，id 2012）并幂等授予 ADMIN；`interaction:favorite/share/comment` V2 已种不重复；不回改 V1–V5。`/interaction/**` 不入 `OperationLogConfig` 拦截路径，互动不写操作日志（与检索/浏览一致）。
  - 客户端：增强 `app/ArticleDetailDialog`（meta 下加互动条 收藏/👍赞/👎踩/分享，按权限码启停，点击后用 `/interaction/state` 或 `/like` 返回刷新；正文下加评论区：树形列表 + 一行输入「发表」+ 每条「回复」走 `QInputDialog`+parentId）；新增 `app/ShareDialog`（`/interaction/users` 填收件人 + 留言 → `POST /share`）、`app/FavoritePage`(route `favorite`「我的收藏」表 + 取消收藏)、`app/SharePage`(route `share`「我的分享」收到/发出双 Tab + 顶部未读数，双击收件箱条 `PUT .../read` 并打开详情)；`MainWindow.cpp` 加 `favorite`/`share` 两路由 + `CMakeLists.txt` 注册三组源。
  - 验证：IDEA 自带 Maven `compile` 通过；后端启动 Flyway 迁至 **v6**（仅新增 `interaction:like`），Bean 装配通过。admin token 接口冒烟全过 —— 收藏 +1/幂等不翻倍/取消 -1、我的收藏列表；点赞 like 73→74、再点取消→73、改踩 dislike 2→3 且 like 不变、改回赞 like+1/dislike-1（双向搬移正确）、COMMENT 点赞 `like_count`+1；评论 顶层+回复 `comment_count` 2→4、评论树 children 正确、删本人评论 -1、中文内容无乱码（`--data-binary @utf8文件`）；分享 `/users` 排除自己与停用用户、`POST /share` 后收件箱出现该条且 `unread-count` 0→1、`PUT .../read` 后回 0、`/share/sent` 含发出条（收件箱/已读环节因坐席角色无权限改用 admin 自分享验证，见下）；`/interaction/state` 与各操作后态一致；测试后已将演示 article 4002 计数清回基线。**已知种子缺口**：当前 `V2`/演示数据仅给 `ADMIN` 授全权，KNOWLEDGE_ADMIN/AUDITOR/AGENT/USER 四内置角色**未授任何权限码**，故坐席账号目前无法访问受保护端点（贯穿模块 1–6 的既有问题，非本模块代码缺陷）；如需坐席实测互动，应另行补这些角色的权限授予。Qt 客户端互动条/评论区/收藏页/分享页待用户自行构建点测。模块 6 后端勾选完成。

- **2026-06-16（续四）· 修复：按分类筛选未含子分类**
  - 问题：检索 `/search/article` 与管理列表 `/knowledge/article` 的 `categoryId` 筛选用精确等值 `.eq(category_id)`，选父/根分类只能命中「直接挂在该分类」的知识，挂在其子分类下的全被漏掉。
  - 修复：`CategoryService` 将原 `delete` 级联在用的私有子树收集逻辑对外暴露为公有 `subtreeIds(rootId)`（返回「自身 + 全部子孙分类 id」）；`SearchService.search` 与 `ArticleService.page` 的分类筛选改为先 `subtreeIds(categoryId)` 展开、再用 `.in(子树ids)` 取代 `.eq`，故选父/根分类可搜全其下所有子分类的知识。两处共用同一语义。`subtreeIds` 至少返回自身 id，不产生空 `IN ()`；`ArticleService`/`SearchService` 注入 `CategoryService` 无循环依赖（`CategoryService` 仅依赖 mapper）。
  - 验证：IDEA 自带 Maven `compile` 通过；后端启动 Flyway 仍 `now at version v5`（无新迁移），Bean 装配全过（容器初始化至启动 Tomcat 才因端口占用报错，已杀旧进程释放 8080）。客户端无改动，前端「知识检索 / 知识管理」分类下拉选父分类即生效。

- **2026-06-16（续三）· 模块 5 检索 + 详情（后端完成 / 客户端待点测）**
  - 后端新增包 `com.kb.knowledge.search`：实体/Mapper `KbViewLog`（`kb_view_log`）、`KbSearchLog`（`kb_search_log`）（纯表，无 BaseEntity，仅 createTime）；`SearchService`（全文检索 + 写检索埋点）、`SearchController`（`/search/article`）。
  - 检索 `GET /search/article`（权限 `knowledge:search`）：`LambdaQueryWrapper` `.eq(status,ONLINE)` + 分类/标签筛选（tagId 先反查 `kb_article_tag`，空集合直接空页）+ 关键词非空时 `.apply("MATCH(title,summary,content) AGAINST ({0} IN NATURAL LANGUAGE MODE)", keyword)`（ngram 全文，自带相关度），无关键词退化为 `view_count desc, online_time desc`；列表富集复用 `ArticleService.toListItems`；每次检索写 `kb_search_log`（含空关键词）。
  - 浏览埋点 `GET /knowledge/article/{id}/view`（权限 `knowledge:search`，置于 `ArticleController`）：`ArticleService.viewDetail` 复用 `detail(id)` 装配 + 原子 `view_count++`（`setSql` 空实体更新，**不触发审计自动填充故不改 update_time**，避免被浏览的知识顶到管理列表最前）+ 写 `kb_view_log`；返回体 `viewCount` 即自增后值。
  - `ArticleService` 重构：从 `page()` 抽出公有 `toListItems(List<KbArticle>)`（批量补 categoryName/authorName/tagNames，避免 N+1），`page()` 与 `SearchService` 共用；构造注入新增 `KbViewLogMapper`。`ArticleController` 补 `clientIp(HttpServletRequest)` 私有方法（与 `AuthController` 同款）。
  - **无新增 Flyway 迁移**：`knowledge:search`（菜单 201）已在 `V2__seed_rbac.sql` 种入且 V2 末尾「ADMIN 授全权」覆盖，`/{id}/view` 复用之；启动 Flyway 仍 `now at version v5`。检索/浏览均 GET，不入 `sys_operation_log`。
  - 客户端：新增 `app/SearchPage.{h,cpp}`（route `knowledge.search`，镜像 `ArticleManagePage`：关键词 + 分类树/标签下拉筛选，回车/「检索」触发，结果表标题/分类/类型/作者/浏览/更新时间，双击进详情）、`app/ArticleDetailDialog.{h,cpp}`（只读详情弹窗，打开即 `GET /{id}/view` 完成浏览埋点，渲染标题/一行 meta/彩色标签/摘要/`QTextEdit` 富文本正文/附件行 + `QFileDialog`→`ApiClient::download` 下载）；`MainWindow.cpp` 路由分支 + `CMakeLists.txt` 注册两组源。
  - 验证：IDEA 自带 Maven `compile` 通过；后端启动 Flyway 仍 v5；admin token 接口冒烟全过 —— 空关键词检索仅回 8 条 ONLINE、`keyword=投诉/话术/退订` 全文命中（1/2/1）、`categoryId`/`tagId`/组合筛选正确、`/{id}/view` 连调两次 `view_count` 386→388（+2）且 `update_time` 不变、`kb_search_log`（含空关键词与 result_count）与 `kb_view_log`（2 行）落库正确。Qt 客户端「知识检索」页与详情弹窗待用户自行构建点测。模块 5 后端勾选完成。

- **2026-06-16（续二）· 模块 4 知识管理后台（完成）**
  - 后端新增包 `com.kb.knowledge.article`：实体/Mapper `KbArticle`（extends BaseEntity）、`KbArticleVersion`/`KbAuditRecord`/`KbArticleTag`（纯表，无逻辑删除）、`KbAttachment`（extends BaseEntity）；DTO `ArticleRequest`/`AuditRequest`/`OfflineRequest`/`ArticleListItemVO`/`ArticleDetailVO`/`TagBriefVO`；`ArticleService`（分页筛选含 tagId 反查 + 批量补 categoryName/authorName/tagNames 避免 N+1；detail 装配标签/附件；create/update/delete/submit/audit/online/offline 状态机；版本快照；syncTags）；`ArticleController`（`/knowledge/article`，`@PreAuthorize` 权限码逐一对应 §4.3）。
  - 状态机：create→DRAFT；update 仅 DRAFT/REJECTED 且 currentVersion++ 写新版本快照；submit→PENDING_AUDIT；audit PASS→ONLINE(写 online_time)/REJECT→REJECTED（均写 `kb_audit_record`）；offline ONLINE→OFFLINE(写 offline_time/reason)；online OFFLINE→ONLINE；非法流转返回 `code=1001`。
  - 附件：新增 `com.kb.knowledge.storage.FileStorage` 抽象 + `LocalFileStorage`（落 `kb.storage.local-dir=./uploads`，`yyyy/MM/uuid.ext`，含路径穿越防护）；`AttachmentService`/`AttachmentController`（`/knowledge/attachment`：`POST /upload` multipart、`GET ?articleId=` 列表、`GET /{id}/download` 流式 + download_count++、`DELETE /{id}` 逻辑删 + 删盘）。
  - 补回模块 3 遗留 TODO：`CategoryService.delete` 级联前校验子树内 `kb_article.category_id` 占用（拦截「分类下存在知识，无法删除」）；`TagService.delete` 校验 `kb_article_tag` 引用（拦截「标签已被知识引用，无法删除」）。
  - `OperationLogInterceptor`：`moduleOf` 加「知识管理」「知识附件」分支；`operationOf` 加 提交审核/审核/上线/下线/上传附件 分支。
  - Flyway `V5__seed_module4_permissions.sql`（幂等，仅权限码）：在菜单 301 下补 `knowledge:article:{delete,submit,offline}`、`knowledge:attachment:{upload,delete}` 五个按钮码并幂等授予 ADMIN；不回改 V1–V4。
  - 客户端：`ApiClient` 扩展 `upload`（`QHttpMultiPart`）/`download`（二进制写盘），`buildRequest` 增 `jsonContentType` 开关；新增 `app/ArticleEditorDialog`（标题/分类/类型/摘要/标签可勾选/`QTextEdit` 富文本 + 加粗/斜体/下划线/H2/项目符号工具栏/附件子面板，新建保存草稿后才可传附件）、`app/ArticleManagePage`（route `knowledge.manage`，筛选栏 + 列表 + 新增/编辑/删除/提交审核/上线/下线工具栏，按权限码启停）、`app/AuditCenterPage`（route `audit`，待审列表 + 正文预览 + 通过/驳回）；`MainWindow` 路由分支 + `CMakeLists.txt` 注册三组源。
  - 验证：IDEA 自带 Maven `compile` 通过；重启后端 Flyway 迁至 **v5**；admin token 接口冒烟全过 —— 状态机 create→submit→audit{PASS}→offline→online 全流转、`audit{REJECT}`→REJECTED、update 版本号 1→2 且 `versions` 返回两版、`status=ONLINE` 筛选、附件 upload→list→download（落盘字节 `diff` 一致）→delete、删占用分类/标签均被 `1001` 拦截、操作日志模块/动作分支正确（知识管理/知识附件 + 提交审核/审核/上线/下线/上传附件）。Qt 客户端「知识管理 / 审核中心」两页点测通过。模块 4 勾选为完成。

- **2026-06-16（续）· 演示/测试数据**
  - 新增独立演示数据脚本 `server/src/main/resources/db/demo/demo_data.sql`（**非 Flyway 迁移**，不占版本号，模块 4 真实 `V5` 留给业务）。幂等可重跑：开头按「演示 ID 段」（部门 1001+/用户 1001+/分类 2001+/标签 3001+/文章 4001+/附件 5001+/版本 6001+/审核 7001+/关系与日志 41000+~52000+/开放应用 8001+/导入 9001+）DELETE 后重插，不触碰 V2 种子与运行期手建数据。
  - 数据围绕客服知识库业务、引用真实主键（机构 1/admin 1/角色 1–5）：4 部门 + 6 人员（演示账号密码均 `123456`，含停用示例）、12 分类树、8 标签、**12 篇文章覆盖全状态**（ONLINE/PENDING_AUDIT/DRAFT/OFFLINE/REJECTED，含 SCRIPT/PRODUCT/TRAIN/OFFICE）、8 版本/5 审核/3 附件、收藏/分享/评论/点赞、近 14 天浏览/搜索/日统计/下载日志、2 开放应用 + 2 导入记录 + 登录/操作/API 日志。
  - 已实测：用 `mysql -u kb -p --default-character-set=utf8mb4 kb < ...demo_data.sql` 三次加载 EXIT=0、中文无乱码、重跑行数不翻倍（幂等验证通过）。加载命令同步补入 README「构建与运行」。


- **2026-06-16**
  - 创建后端 `server/` 骨架：`pom.xml`（Spring Boot 3.3.5 + Security + MyBatis-Plus 3.5.9 + Flyway + jjwt 0.12.6 + springdoc）、`KbApplication`。
  - `common`（`Result`/`ResultCode`/`PageResult`/`BusinessException`/`BaseEntity`/全局异常）、`config`（MyBatis-Plus 分页+自动填充、OpenAPI）、`security`（JWT 过滤器/Provider、`SecurityConfig` 无状态、`LoginUser`/`SecurityUtils`）。
  - `system/auth`：骨架阶段先由 `AuthController` stub `admin/123456` 发 JWT、`/auth/me` 返回 stub 用户+16 菜单+21 权限码；随后模块 1 已替换为 DB+BCrypt+RBAC 真实实现。`knowledge/search/interaction/statistics/openapi/ai/realtime` 七个 `package-info` 占位。
  - Flyway `V1__init_schema.sql`（一期 26 表 + 索引 + kb_article ngram 全文索引）、`V2__seed_rbac.sql`（根机构、admin[BCrypt 123456]、5 内置角色、菜单/权限码树，ADMIN 授全权）。
  - `application.yml`：端口 8080 / context-path `/api`、MySQL(`createDatabaseIfNotExist`)、Flyway、MyBatis-Plus 逻辑删除、Swagger、actuator health、`kb.jwt/storage/ai` 配置。
  - 修复 MyBatis-Plus 3.5.9 分页依赖：补 `mybatis-plus-jsqlparser`（3.5.9 起 `PaginationInnerInterceptor` 等拆出独立模块，starter 不再传递引入）。
  - 启动验证通过：建 `kb` 库 + 专属账号 `kb`（app 用 `kb` 不用 root）；IDEA 自带 Maven 启动 `KbApplication`，Flyway 建 26 表+RBAC 种子，`/health` / `/auth/login` / `/auth/me` 全绿。
  - 搭建客户端 `knowledgeAnswer/` 骨架：`core/`（ApiClient 统一响应+注入 JWT、Session 会话+权限码、Settings、Logger、Notify）、`app/`（LoginDialog 接 `/auth/login`+`/auth/me`、MainWindow 顶栏+导航树+内容区、PageRouter 懒加载路由、Application 登录↔主窗口编排、PlaceholderPage 占位页）、`resources/styles/app.qss` 全局主题（高效简洁、落落大方，应用风格基线）。CMake 加 `Network` 组件 + 源码根 include。删除向导旧 `mainwindow.{h,cpp,ui}`。**待 Qt Creator 编译联调验证**。
  - 搭建 AI 服务 `ai-service/` 占位骨架（Python 3.11 + FastAPI，**conda 环境 `environment.yml`/`requirements.txt`，env `kb-ai`**）：`app/main.py`（路由装配 + 根路由）、`app/api/health.py`（`/health`）、`app/api/ai.py`（`/ai/parse`·`/ai/embed`·`/ai/qa` 占位 501）、`app/schemas/ai.py`（pydantic 契约：Parse/Embed/Qa 请求响应）、`app/core/`（config/logging）、`app/services/`（二期空包）+ `README.md`。已 `py_compile` 全过；`/health` 运行冒烟待 conda 环境建好验证。
  - 迁入团队 Git 仓库 `auto-service-knowledge-base/`：源码、文档、需求图片、根 README 与 `.gitignore` 已放入仓库并完成首次提交；上层 `thePro/` 只保留 `.claude/` 与仓库目录；`knowledgeAnswer/build`、`server/target`、`__pycache__` 等构建/缓存产物不提交。
  - 完成第一阶段·模块 1 登录鉴权 + RBAC：新增 `SysUser/SysRole/SysPermission` 实体与 Mapper、`PermissionService`（实现 `UserPermissionResolver`）、`AuthService`、`MenuTreeBuilder`；`AuthController` 登录改为 DB + BCrypt，`/auth/me` 改为 DB 装配；`JwtAuthenticationFilter` 改为解析 token 后加载用户权限码写入 `SimpleGrantedAuthority`，支撑 `@PreAuthorize`；禁用/过期账号登录与旧 token 均被拦截。IDEA 自带 Maven `compile` 通过；接口冒烟：`/health` UP、`admin/123456` 登录成功、错误密码返回 `code=1001` 且无 token、`/auth/me` 返回 ADMIN + 21 权限码 + 6 顶级目录/16 菜单。
  - 契约与异常处理微调：`/auth/logout` 明确为无状态退出（客户端丢弃 token，服务端不维护黑名单）；`GlobalExceptionHandler` 补 `HttpMessageNotReadableException`，JSON 请求体格式错误返回 `code=1001`。
  - 模块 2「系统管理」初版编码完成：后端新增机构、人员、角色、菜单按钮权限、登录日志、操作日志 API；补 `V3__seed_module2_permissions.sql` 扩充系统管理按钮权限并授予 ADMIN；登录成功/失败写入 `sys_login_log`，`/system/**` 的 POST/PUT/DELETE 由拦截器写入 `sys_operation_log`。客户端新增 `SystemAdminPage`，接入 `system.user`、`system.role`、`system.permission`、`system.log` 四个路由，支持列表刷新、基础新增/编辑/删除、人员启停/改密、角色权限分配、日志查看。已通过 IDEA Maven `compile` 与 Qt CMake/MinGW 构建。
  - **模块 2 验收通过（完成）**：重启 IDEA 后端触发 Flyway 至 `V3`，接口冒烟 **17/0 全绿**（19 条 `system:*` 权限码就位；`/system/org/tree`·`/system/user`·`/system/role`·`/system/permission/tree`·`/system/log/login`·`/system/log/operation` 均 `code=0`；机构 POST→GET→PUT→DELETE 闭环；操作日志/登录日志写入正常）；Qt 客户端人员/角色/菜单按钮权限/系统日志四页逐页点测通过。模块 2 勾选为完成，进入模块 3「知识基础」。
  - 完成第一阶段·模块 3「知识基础」（分类 / 标签）：
    - 后端新增 `com.kb.knowledge.category`（分类树：`KbCategory` 实体/Mapper、`CategoryTreeVO`/`CategoryRequest`、`CategoryService`/`CategoryController`，镜像 `system/org`）与 `com.kb.knowledge.tag`（标签：`KbTag` 实体/Mapper、`TagRequest`、`TagService`/`TagController`，镜像 `system/user` 分页范式）。分类 `GET {"","/tree"}` 树查询 + `GET /{id}`/`POST`/`PUT /{id}`/`DELETE /{id}`；标签分页（`page/pageSize/keyword`）+ `GET /{id}`/`POST`/`PUT /{id}`/`DELETE /{id}`。`code` 表无唯一约束故不做编码唯一校验；标签 `ensureNameUnique` 校验 `uk_tag_name`。
    - Flyway `V4__seed_module3_permissions.sql`：在菜单节点 `303 分类标签管理` 下幂等补 6 条按钮权限码（`knowledge:category:{create/update/delete}`、`knowledge:tag:{create/update/delete}`）并幂等授予 ADMIN（`knowledge:category:list`/`knowledge:tag:list` 读权限 V2 已种）。
    - 操作日志接入 `/knowledge/**`：`OperationLogConfig` 拦截路径加 `/knowledge/**`；`OperationLogInterceptor.moduleOf()` 新增「知识分类」「知识标签」两分支。
    - 客户端新增 `app/KnowledgeBasePage.{h,cpp}`（route=`category`「分类标签管理」单页双 Tab）：分类 Tab 用 `QTreeWidget`（工具栏 新增根/新增子/编辑/删除/刷新），标签 Tab 用 `QTableWidget`（颜色色块 + 新增/编辑弹窗用 `QColorDialog` 取色）；按 `Session::hasPermission` 控制按钮；接入 `MainWindow` 路由分支与 `CMakeLists.txt` 构建源。
    - **需求变更：分类删除由「阻断含下级」改为「级联删除」**——后端 `CategoryService.delete` 改为收集选中节点自身及其全部子孙逐个逻辑删除；客户端对含子节点的分类要求**输入「确定删除」**二次确认（`QInputDialog`），无子节点仍走普通确认弹窗。
    - **修复全项目通病：软删行同名重建返回 5000**——`GlobalExceptionHandler` 新增 `@ExceptionHandler(DataIntegrityViolationException.class)`，把唯一索引冲突（软删行与新建行同名等）兜底成友好的 `code=1001 数据已存在或违反唯一约束`，一处覆盖 org/user/role/tag/category；服务层 `ensureNameUnique` 对正常活跃重名仍给精确提示。软删名因此保持「占用不可复用」（暂可接受）。
    - 后端冒烟通过（分类级联删除前后行数核对、标签活跃重名 1001、软删重名 1001 兜底），Qt 客户端双 Tab 点测通过；模块 3 勾选为完成，进入模块 4「知识管理后台」。

- **2026-06-15**
  - 确认客户端为 Qt Widgets 工程（Qt 6.11.1 / MinGW）。
  - 创建 `docs/总体开发计划.md`、`docs/开发进度.md`。
  - 创建 `docs/数据库表清单.md`、`docs/API契约.md`、根目录 `README.md`、`.gitignore`。
  - 文件归档：3 张需求图移入 `docs/需求图/`（重命名 需求-01/02/03）；需求文档移入 `docs/`；删除误建的 `docs/需求清单.md`。

---

## 六、下一步（建议顺序）

1. ✅ 已补齐 docs（`数据库表清单.md`/`API契约.md`）+ 根目录 `README.md`/`.gitignore`；需求图、需求文档已归入 `docs/`。
2. ✅ 后端 `server/` 骨架完成并**已启动验证通过**：Spring Boot 工程 + `common/config/security` + `auth`（login/me）+ Flyway `V1`(26 表)`V2`(RBAC 种子) + `application.yml` + `/actuator/health`。用 **IDEA 自带 Maven** 启动 `KbApplication`：Flyway 建表至 v2 / `/api/actuator/health`=UP / `admin/123456` 发 JWT / `/auth/me` 返回菜单+权限码。
3. ✅ 客户端骨架完成：在 `knowledgeAnswer/` 上扩展 `core` + `app` + `resources`，登录 → 主窗口（导航树 + QStackedWidget + PageRouter）+ 占位页 + 全局主题。**待用 Qt Creator 编译运行**（若构建缓存报错，删 `build/` 重新 Configure；CMake 已新增 `Network` 组件）。
4. ✅ 搭 `ai-service/` 占位完成：FastAPI `app/main.py` + `/health` + `/ai/*` 契约占位（501）；conda 环境 `environment.yml`（env `kb-ai`）。`py_compile` 通过，`/health` 冒烟待 conda 环境建好验证。
5. ✅ 第一阶段·模块 1（登录鉴权 + RBAC）完成：后端已从 stub 切为 DB + BCrypt + JWT + RBAC，`/auth/me` 从 DB 装配菜单/权限码，JWT 过滤器注入 authorities；已编译与接口冒烟。
6. ✅ 团队仓库 `auto-service-knowledge-base/` 首次结构落地完成并提交，构建/缓存产物已由 `.gitignore` 排除。
7. ✅ 模块 2「系统管理」完成：后端机构/人员/角色/菜单按钮权限/日志 API 接口冒烟 17/0 通过，Qt 客户端四页点测通过，已勾选完成。
8. ✅ 模块 3「知识基础」完成：后端分类（树）`/knowledge/category` + 标签 `/knowledge/tag` CRUD 冒烟通过（分类级联删除、标签活跃重名/软删重名兜底已验证），Qt 客户端「分类标签管理」双 Tab 点测通过，已勾选完成。
9. ✅ 模块 4「知识管理后台」完成：后端知识 `/knowledge/article`（CRUD + 版本/审核/上下线状态机）+ 附件 `/knowledge/attachment`（本地存储上传/下载）+ 占用校验补回 + Flyway V5 权限码，接口冒烟全过；Qt 客户端「知识管理 / 审核中心」两页 + 富文本编辑弹窗点测通过，已勾选完成。
10. ✅ 模块 5「检索 + 详情」后端完成：`/search/article`（MySQL 全文检索 `ft_art_content` + 分类/标签筛选，分页，仅 ONLINE，写 `kb_search_log`）+ 知识详情查看并埋点浏览（`/knowledge/article/{id}/view`，原子 `view_count++` + 写 `kb_view_log`），无新增 Flyway 迁移；接口冒烟全过（全文命中、筛选、浏览 +2、埋点落库）。Qt 客户端「知识检索」页 + 只读详情弹窗已编码，待用户自行构建点测。
11. ✅ 模块 6「知识互动」后端完成：`/interaction/*`（收藏 / 点赞·点踩 / 评论 / 分享 + 站内通知收件箱 + `/users` 选人 + `/state` 初态）+ Flyway V6 补 `interaction:like`，接口冒烟全过（收藏幂等、点赞双向搬移、评论树、分享收件箱/未读/标已读，演示数据已清回基线）。Qt 客户端详情弹窗互动条+评论区、「我的收藏」「我的分享」两页已编码，待用户自行构建点测。
12. 模块 7「数据统计」：仪表盘汇总 / 热门知识 / 热门搜索词（`/statistics/*`，权限 `statistics:view`）；按分类聚合需含子分类（复用 `CategoryService.subtreeIds`）。
